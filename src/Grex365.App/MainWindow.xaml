<ui:FluentWindow x:Class="Grex365.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:Grex365.App.ViewModels"
        xmlns:models="clr-namespace:Grex365.Core.Models;assembly=Grex365.Core"
        mc:Ignorable="d"
        Title="GREX365 · Microsoft 365 toolkit"
        Height="800" Width="1280"
        MinHeight="600" MinWidth="1000"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="GREX365" />

        <Grid Grid.Row="1" Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Conexión a Microsoft 365"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
                <Border CornerRadius="8" Padding="12,8" Margin="0,0,12,0"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                                 Fill="{Binding Connect.GraphConnected, Converter={StaticResource BoolToBrushConverter}}" />
                        <TextBlock Text="Microsoft Graph" FontWeight="SemiBold" />
                        <TextBlock Margin="8,0,0,0"
                                   Text="{Binding Connect.GraphConnected, Converter={StaticResource BoolToOnOffConverter}}" />
                    </StackPanel>
                </Border>

                <Border CornerRadius="8" Padding="12,8"
                        Background="{DynamicResource ControlFillColorDefaultBrush}">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                                 Fill="{Binding Connect.ExchangeConnected, Converter={StaticResource BoolToBrushConverter}}" />
                        <TextBlock Text="Exchange Online" FontWeight="SemiBold" />
                        <TextBlock Margin="8,0,0,0"
                                   Text="{Binding Connect.ExchangeConnected, Converter={StaticResource BoolToOnOffConverter}}" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16">
                <ui:Button Content="Connect"
                           Appearance="Primary"
                           Margin="0,0,8,0"
                           Command="{Binding Connect.ConnectCommand}" />
                <ui:Button Content="Cancel"
                           Margin="0,0,8,0"
                           Command="{Binding Connect.CancelCommand}" />
                <ui:Button Content="Disconnect"
                           Command="{Binding Connect.DisconnectCommand}" />
                <TextBlock Margin="16,0,0,0"
                           VerticalAlignment="Center"
                           Text="{Binding Connect.StatusMessage}"
                           Opacity="0.8" />
            </StackPanel>

            <Border Grid.Row="3"
                    CornerRadius="8"
                    Padding="0"
                    Background="{DynamicResource ControlFillColorDefaultBrush}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="Log"
                               FontWeight="SemiBold"
                               Margin="12,8" />
                    <ListBox ItemsSource="{Binding LogEntries}"
                             Background="Transparent"
                             BorderThickness="0"
                             VirtualizingPanel.IsVirtualizing="True"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:LogEntry}">
                                <Grid Margin="8,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}"
                                               Opacity="0.6"
                                               FontFamily="Cascadia Mono,Consolas" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Severity}"
                                               FontWeight="SemiBold"
                                               Foreground="{Binding Severity, Converter={StaticResource SeverityToBrushConverter}}" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Source}"
                                               Opacity="0.8" />
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding Message}"
                                               TextWrapping="Wrap" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="GREX365 · v0.1.0-alpha" Opacity="0.6" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</ui:FluentWindow>
