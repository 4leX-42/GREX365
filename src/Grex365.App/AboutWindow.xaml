<ui:FluentWindow x:Class="Grex365.App.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Grex365.App"
        mc:Ignorable="d"
        Title="Acerca de GREX365"
        Height="420" Width="560"
        ResizeMode="NoResize"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Acerca de" />

        <StackPanel Grid.Row="1" Margin="32,12,32,16" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <ContentControl Style="{StaticResource GxLogoBadge}"
                                Width="56" Height="56"
                                Margin="0,0,18,0"
                                VerticalAlignment="Center" />
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="GREX365" FontSize="28" FontWeight="Light" />
                    <TextBlock x:Name="VersionText" FontSize="12" Opacity="0.6"
                               FontFamily="Cascadia Mono, Consolas" />
                </StackPanel>
            </StackPanel>

            <TextBlock TextWrapping="Wrap"
                       Margin="0,12,0,0"
                       Opacity="0.85"
                       Text="Toolkit para operaciones administrativas sobre Microsoft 365: Exchange Online, Microsoft Graph y Entra ID. Autenticación por certificado (app-only) o device code (delegado)." />

            <Border CornerRadius="8" Padding="14" Margin="0,16,0,0"
                    Background="{DynamicResource ControlFillColorDefaultBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Versión" Opacity="0.7" />
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="VersionDetailText" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text=".NET runtime" Opacity="0.7" Margin="0,6,0,0" />
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="RuntimeText" Margin="0,6,0,0" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Datos en" Opacity="0.7" Margin="0,6,0,0" />
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="DataDirText"
                               Margin="0,6,0,0"
                               TextWrapping="Wrap"
                               FontFamily="Cascadia Mono,Consolas"
                               FontSize="12" />
                </Grid>
            </Border>
        </StackPanel>

        <Border Grid.Row="2"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                Padding="24,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Abrir carpeta de datos"
                           Margin="0,0,8,0"
                           Click="OpenDataDir_Click" />
                <ui:Button Content="Cerrar"
                           Appearance="Primary"
                           Click="Close_Click" />
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
