<UserControl x:Class="Grex365.App.Views.ConnectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:models="clr-namespace:Grex365.Core.Models;assembly=Grex365.Core"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Conexión a Microsoft 365"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border CornerRadius="8" Padding="12,8" Margin="0,0,12,0"
                    Background="{DynamicResource ControlFillColorDefaultBrush}">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                             Fill="{Binding GraphConnected, Converter={StaticResource BoolToBrushConverter}}" />
                    <TextBlock Text="Microsoft Graph" FontWeight="SemiBold" />
                    <TextBlock Margin="8,0,0,0"
                               Text="{Binding GraphConnected, Converter={StaticResource BoolToOnOffConverter}}" />
                </StackPanel>
            </Border>

            <Border CornerRadius="8" Padding="12,8"
                    Background="{DynamicResource ControlFillColorDefaultBrush}">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Margin="0,0,8,0"
                             Fill="{Binding ExchangeConnected, Converter={StaticResource BoolToBrushConverter}}" />
                    <TextBlock Text="Exchange Online" FontWeight="SemiBold" />
                    <TextBlock Margin="8,0,0,0"
                               Text="{Binding ExchangeConnected, Converter={StaticResource BoolToOnOffConverter}}" />
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16">
            <ui:Button Content="Connect"
                       Appearance="Primary"
                       Margin="0,0,8,0"
                       Command="{Binding ConnectCommand}" />
            <ui:Button Content="Cancel"
                       Margin="0,0,8,0"
                       Command="{Binding CancelCommand}" />
            <ui:Button Content="Disconnect"
                       Command="{Binding DisconnectCommand}" />
            <TextBlock Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding StatusMessage}"
                       Opacity="0.8" />
        </StackPanel>

        <Border Grid.Row="3"
                CornerRadius="8"
                Padding="0"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="Tenant"
                           FontWeight="SemiBold"
                           Margin="12,8" />
                <Grid Margin="12,0,12,12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Tenant ID" Opacity="0.7" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TenantId, FallbackValue=—}" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Dominio" Opacity="0.7" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TenantDomain, FallbackValue=—}" />
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Cuenta" Opacity="0.7" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Account, FallbackValue=—}" />
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
