<UserControl x:Class="Grex365.App.Views.MailFlowRulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d">
    <Grid Margin="32,28,32,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
            <Border Width="44" Height="44" CornerRadius="12"
                        Background="{StaticResource BrandAccentGradientSoft}"
                        BorderBrush="{StaticResource GlassBorderHighlight}"
                        BorderThickness="1"
                        Margin="0,0,16,0"
                        Effect="{StaticResource AccentGlowSoftEffect}">
                    <TextBlock Text=""
                               FontFamily="{StaticResource IconFontFamily}"
                               FontSize="22"
                               Foreground="{StaticResource BrandAccentSolid}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
            <TextBlock Text="Mail flow rules" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock Grid.Row="1"
                   Text="Transport rules de Exchange Online (Get-TransportRule). Solo lectura en esta version."
                   Opacity="0.7" Margin="0,0,0,16" />

        <Border Grid.Row="2" CornerRadius="8" Padding="12" Margin="0,0,0,16"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0"
                           Content="Cargar"
                           Appearance="Primary"
                           Command="{Binding LoadCommand}" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Filtro:" VerticalAlignment="Center" Margin="0,0,6,0" Opacity="0.8" />
                    <ui:TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                                Width="240"
                                PlaceholderText="nombre o descripcion..." />
                </StackPanel>
                <ui:ProgressRing Grid.Column="2"
                                 IsIndeterminate="True"
                                 Width="20" Height="20"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}" />
            </Grid>
        </Border>

        <Border Grid.Row="3" CornerRadius="8" Padding="0"
                Background="{DynamicResource ControlFillColorDefaultBrush}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Bottom" Margin="12,8" Opacity="0.8" Text="{Binding StatusMessage}" />
                <DataGrid ItemsSource="{Binding Filtered}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="Transparent"
                          BorderThickness="0"
                          RowBackground="Transparent"
                          AlternatingRowBackground="{DynamicResource SubtleFillColorTertiaryBrush}"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          VirtualizingPanel.IsVirtualizing="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Prio"        Binding="{Binding Priority}"     Width="60"  ElementStyle="{StaticResource DataCellText}" />
                        <DataGridTextColumn Header="State"       Binding="{Binding State}"        Width="80"  ElementStyle="{StaticResource DataCellText}" />
                        <DataGridTextColumn Header="Mode"        Binding="{Binding Mode}"         Width="90"  ElementStyle="{StaticResource DataCellText}" />
                        <DataGridTextColumn Header="Name"        Binding="{Binding Name}"         Width="260" ElementStyle="{StaticResource DataCellText}" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}"  Width="*"   ElementStyle="{StaticResource DataCellText}" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
