<UserControl x:Class="Grex365.App.Views.DomainCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:l="clr-namespace:Grex365.App.Xaml"
             xmlns:models="clr-namespace:Grex365.Core.Models;assembly=Grex365.Core"
             mc:Ignorable="d">
    <Grid Margin="22,18,22,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,4">
            <TextBlock Text="{l:L10n Key=DomainCheck.Eyebrow}" Style="{StaticResource EyebrowText}" />
            <TextBlock Text="{l:L10n Key=DomainCheck.Title}" Style="{StaticResource PageHeroTitle}" VerticalAlignment="Center" />
        </StackPanel>
        <TextBlock Grid.Row="1"
                   Text="{l:L10n Key=DomainCheck.Subtitle}"
                   Style="{StaticResource PageSubtitleText}" />

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0"
                        Text="{Binding Domain, UpdateSourceTrigger=PropertyChanged}"
                        PlaceholderText="{l:L10n Key=DomainCheck.Placeholder}">
                <ui:TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding RunCommand}" />
                </ui:TextBox.InputBindings>
            </ui:TextBox>
            <ui:Button Grid.Column="1"
                       Content="{l:L10n Key=DomainCheck.Run}"
                       Appearance="Primary"
                       Margin="8,0,0,0"
                       Command="{Binding RunCommand}" />
            <ui:Button Grid.Column="2"
                       Content="{l:L10n Key=DomainCheck.Cancel}"
                       Margin="8,0,0,0"
                       Command="{Binding CancelCommand}" />
            <ui:ProgressRing Grid.Column="3"
                             IsIndeterminate="True"
                             Width="20" Height="20"
                             Margin="8,0,0,0"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}" />
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="{Binding StatusMessage}"
                           Opacity="0.8"
                           Margin="12,8" />
                <ListView ItemsSource="{Binding Records}"
                          Background="Transparent"
                          BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="{l:L10n Key=DomainCheck.Column.Type}" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Type}" Style="{StaticResource DataCellText}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{l:L10n Key=DomainCheck.Column.Status}" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Status}" Style="{StaticResource DataCellText}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{l:L10n Key=DomainCheck.Column.Value}" Width="700">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Value}" Style="{StaticResource DataMonoCellText}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
